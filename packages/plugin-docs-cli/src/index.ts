// This package is a CLI tool, not a library
// No exports - use the binary instead:
// npx @grafana/plugin-docs-cli serve ./docs

// If you need programmatic access to parsing, use:
// import { parseMarkdown } from '@grafana/plugin-docs-renderer';

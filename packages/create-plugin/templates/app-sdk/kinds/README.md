# Kinds (grafana-app-sdk)

See [`.config/app-sdk/README.md`](../.config/app-sdk/README.md) for the full workflow.

After regenerating kinds, run `{{ packageManagerName }} run generate:api-client` (or `{{ packageManagerName }} run generate`
for both) to refresh the RTK Query clients in `src/api/generated/`. See `.config/app-sdk/README.md`.

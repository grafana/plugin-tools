apiVersion: 1

apps:
  - type: '{{ pluginId }}'
    org_id: 1
    org_name: '{{ orgName }}'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key

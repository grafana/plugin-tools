export const PLUGIN_ID = WEBPACK_PLUGIN_ID;
export const PLUGIN_VERSION = WEBPACK_PLUGIN_VERSION;
export const PLUGIN_BASE_URL = `/a/${PLUGIN_ID}`;

export enum ROUTES {
  One = 'one',
  Two = 'two',
  Three = 'three',
  Four = 'four',
}

apiVersion: 1

datasources:
  - name: '{{ pluginName }}'
  - type: '{{ normalize_id pluginName orgName 'datasource' }}'
    access: proxy
    isDefault: true
    orgId: 1
    version: 1
    editable: true
    uid: U9sW5bank

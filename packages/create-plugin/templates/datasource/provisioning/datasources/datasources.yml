apiVersion: 1

datasources:
  - name: '{{ pluginName }}'
    type: '{{ normalize_id pluginName orgName 'datasource' }}'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
